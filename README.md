## Development Setup

1. Copy `.env.example` to `.env`. Update `.env` to match your development
   environment.
1. Run `./composer.phar install` to install project dependencies. Any changes to
   `composer.json` and `composer.lock` files should be committed to Git.
1. Run `npm install` to install JavaScript dependencies.
